package com.intglobal.etp;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class EmployeeTransferPortalApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the Spring application context starts.
        // Requires a reachable PostgreSQL instance configured via the ETP_DB_* environment variables.
    }
}
