# Prompt History — EmployeeTransferPortal

Chronological audit log of user prompts, change requests, and AI execution turns.

**CRITICAL — APPEND-ONLY:** This file must never be overwritten. Read the existing content first, then append each new entry beneath all previous entries, using the format defined in `.agent/rules/auto-log.md`.

---

### [2026-09-18 00:00]
**User Request:** Initialize the EmployeeTransferPortal project using the INT AI-First project setup standard.
**Agent Action:** Ran the discovery gate and confirmed: Backend Only, Modular Monolith (Microservice Ready), Java + Spring Boot 4.x with Maven, PostgreSQL with Hibernate/Spring Data JPA, Spring Security + JWT, Docker deployment target, Gate 1 reviewer Soumyadeep Adhikary, Gate 2 reviewer TBD, no BRD supplied, build targeting Java 21 with Java 25 as the intended target, and no `git init`. Copied the INT Control Plane verbatim into `.agent/` (13 files). Copied 7 SDD skills into `.agents/skills/`. Created `AGENTS.md` and `.gitignore`. Generated the `.ai-context/` knowledge base: `constitution.md`, `project_context.md`, `architecture.md`, `BRD.md`, `brd-change-log.md`, `status.md`, `prompt_history.md`, 10 artifact subdirectories with `.gitkeep`, and 12 templates. Created the execution layer `src/backend/`, `tests/backend/{modules,config,shared}/` and `docs/`, with a minimal runnable Spring Boot baseline under base package `com.intglobal.etp`. No business modules and no specs were created — spec generation is blocked pre-Gate 0.

### [2026-09-18 20:02]
**User Request:** Ingest the BRD for the Employee Transfer Portal.
**Agent Action:** Extracted `docs/Requirement for SDD.docx` ("SDD Developer Assessment — Employee Internal Transfer Digital Journey") via OOXML text extraction after finding pandoc unavailable. Identified the source as an assessment brief carrying a real business requirement in sections 2-3 wrapped in assessment scaffolding. Confirmed two scope decisions with the project owner: Backend Only with the portal UI as an external consumer, and downstream integrations recorded as an open question with a stated stubbed-adapter assumption. Rebuilt `.ai-context/BRD.md` to v1.0 with 18 functional requirements (BRD-001 to BRD-018), 3 business rules, 5 assumptions, 6 out-of-scope items, 5 business-level acceptance criteria, 14 open questions typed as business or technical decisions, and 6 document conflicts (DC-01 to DC-06). Recorded that the source supplies no non-functional requirements and no Constitution section; invented none. Set BRD status to Pending Review and staged the Gate 0 review record at `.ai-context/pr_reviews/BRD-20260918-200238.md`. Appended a 5-module business domain proposal to `.ai-context/architecture.md` §9 — proposal only, no module folders created. Added a Governance & Roles section to `.ai-context/constitution.md` and flagged three new items for human review. Logged the ingestion in `.ai-context/brd-change-log.md` and updated `.ai-context/status.md`. HALTED per `.agent/rules/int-standards.md` §6 — spec generation remains blocked pending Gate 0 approval.

### [2026-09-18 20:15]
**User Request:** Ask the BRD open questions one at a time with options, so the answers can be chosen.
**Agent Action:** Presented all 14 open questions individually via option prompts with previews. Author selected: OQ-01 hardcoded five-rule eligibility set; OQ-02 eleven-state lifecycle with rejection and withdrawal; OQ-03 mixed model — Manager and HR human via REST, downstream four as stubbed outbound ports; OQ-04 org-data update as hard gate then parallel fan-out; OQ-05 reference data owned locally by `employee-directory`; OQ-06 effective date between +30 and +365 days; OQ-07 in-portal notification only; OQ-08 withdraw until APPROVED with terminal rejection; OQ-09 one open request, immutable after submit; OQ-10 INT tiered NFR defaults marked Provisional; OQ-11 confidential HR data with masking, encryption at rest, full audit and 7-year retention; OQ-12 immutable append-only audit trail with role-scoped reads; OQ-13 bounded retry then park, no compensation; OQ-14 employee/manager/HR read scoping. Rewrote `.ai-context/BRD.md` §9 as proposed resolutions (v1.1), adding 6 derived requirements BRD-019 to BRD-024 and 7 consequences C-01 to C-07, and flagged every invented threshold as unsupported by the source document. Revised `architecture.md` §9.6 to §9.8 — six modules now including `audit`, four ADRs required, saga explicitly ruled out. Updated `constitution.md` to v1.2 with Provisional NFR targets, coverage floors and data-protection rules DP-01 to DP-05, plus an amendment log. Refreshed the Gate 0 review record, `brd-change-log.md` entry 3, and `status.md`. Recorded throughout that these are author proposals, not approvals, since author is not reviewer. Spec generation remains blocked pending Gate 0.

### [2026-09-18 20:47]
**User Request:** Assert Gate 0 approver authority and approve the BRD baseline.
**Agent Action:** Halted to resolve a governance conflict before recording any approval. Identified three issues: the Gate 0 roster named Soumyadeep Adhikary, an inference made during project setup that was never confirmed with the user (setup asked only about Gate 1 and Gate 2); `author != reviewer` applied because the approver had authored all 14 resolutions; and three different email identities were in play (roster soumyadeep@intglobal.com, session pritam.naskar@intglobal.com, git vaibhaw.soni@intglobal.com) with no git repository, so §6 verification could not run. User confirmed: Gate 0 ownership is theirs, approving identity is vaibhaw.soni@intglobal.com, and it is the same person as the recorded author. Initialised the git repository, which resolved identity to Vaibhaw Soni / vaibhaw.soni@intglobal.com and made the §6 email check PASS against the corrected roster. Rewrote author attribution from Pritam Naskar to Vaibhaw Soni across BRD.md, project_context.md, constitution.md, prompt_history.md and the review record. Corrected the Gate 0 roster entry. Recorded Gate 0 APPROVAL: BRD.md to v1.2 Approved, all 14 resolutions and 6 derived requirements Ratified, A-03 Confirmed, C-01..C-07 accepted, DC-01..DC-06 acknowledged. Logged the `author != reviewer` waiver explicitly in a dedicated Governance Waiver section naming the waiver scope, the person, and every value approved without independent scrutiny. Updated status.md (spec generation UNBLOCKED), brd-change-log.md entry 4, and constitution.md core governance rules.

### [2026-09-18 21:02]
**User Request:** Begin the SDD feature lifecycle now that Gate 0 is approved.
**Agent Action:** Ran reviewer detection — Git identity `vaibhaw.soni@intglobal.com` does not match the Gate 1 roster (`soumyadeep@intglobal.com`), so Option 1 (Review Pending Specs) is unavailable and the user proceeds as spec author. No Gate 1 approved spec existed, so development could not start; entry point was spec authoring. User ratified the slicing strategy: six specs, one per module in `architecture.md` §9.7, authoring the first only before halting. Selected `employee-directory` as the first spec on dependency grounds — it is the only module with no upstream dependency, and `transfer-request` cannot express testable validation or authorization criteria without it. Authored `.ai-context/specs/employee-directory.spec.md` with 5 API contracts (4 HTTP, 1 published in-process interface), a 4-table data model, 14 acceptance criteria and 16 spec-derived unit test cases, full BRD traceability, 8 out-of-scope items and 6 open dependencies. Resolved BRD consequence C-01 by proposing a four-column extension to the `employees` schema rather than narrowing eligibility rules ELIG-01/02/04/05. Flagged three items for reviewer ruling: the three list endpoints are derived rather than extracted from BRD-002 to BRD-004; `/api/v1` path versioning is not yet ratified in the constitution; and the module's Utility 60% coverage tier may need raising to Critical 80% because it supplies authorization data. Created `.ai-context/dashboard.html` from the template as the single source of truth, recording the Gate 0 record and the pending Gate 1 review. Updated `.ai-context/status.md` with the Active Specs board and Daily Execution Log, and corrected a stale Gate 0 status block in `project_context.md`. HALTED per the Gate 1 turn-termination rule — no plan, tasks, test cases or implementation code generated.

### [2026-09-19 12:12]
**User Request:** Review the BRD and the `employee-directory` spec for ambiguities and missing detail, assess code-generation readiness, then apply the fixes and raise the write-path contradiction as a BRD amendment.
**Agent Action:** Re-read `.ai-context/BRD.md` and `.ai-context/specs/employee-directory.spec.md` and assessed the spec as **not code-ready**. Reported four code-generation blockers, one BRD-level contradiction, four internal inconsistencies and five testability gaps. Revised the spec to **rev 2**: defined JWT principal resolution (subject claim matched case-insensitively against `employees.email`, stated as a hard contract on the future auth spec); specified the `EmployeeView` and `EmployeeEligibilityAttributes` published records; separated the two meanings of "role" by renaming the `roles` table to `job_positions` and introducing an explicit `employee_authorities` table for `ROLE_EMPLOYEE`/`ROLE_MANAGER`/`ROLE_HR`; specified a concrete seed set of 6 departments, 5 locations, 6 job positions and 6 employees with fixed absolute dates and `example.com` addresses. Corrected the C-01 count from four attributes to five, restored `joined_at` which rev 1 had dropped against ratified OQ-05, fixed the UT09 empty-body contradiction, and retargeted the N+1 criterion from the flat list endpoints to `/employees/me`. Added deterministic ordering, a 403 criterion, mandated log statements so the no-PII check cannot pass vacuously, explicit rejection of invalid pagination rather than Spring's silent clamping, a required `@RestControllerAdvice` for the error envelope, and an injectable `Clock` constraint passed forward to the `approval` spec. Acceptance criteria renumbered to AC01–AC19 with 23 tests. Raised **AMD-01** against the approved BRD: ratified OQ-03/OQ-04 and OQ-05 contradict each other on the org-data write path, leaving BRD-014 unimplementable and ELIG-03 permanently failing open. Recorded three resolution options without selecting one, since that is a Gate 0 ruling. BRD to v1.3, status Approved with one open amendment; cross-referenced at both contested resolutions and at BRD-014. Synced `brd-change-log.md` entry 5, `architecture.md` §9.9, `status.md` block 7 and daily log, and `dashboard.html`. No plan, tasks, test cases or code generated — Gate 1 HALT still in force.

### [2026-09-19 12:26]
**User Request:** Rule on AMD-01 by choosing from presented options.
**Agent Action:** Presented the three AMD-01 resolution options; Gate 0 approver Vaibhaw Soni ruled **Option A** at 12:26:07. Recorded the ruling in `.ai-context/BRD.md` (v1.4) with a full effects table: OQ-05 confirmed unchanged, OQ-03 narrowed so organisational-data update is an in-process call rather than a stubbed external adapter (Payroll, IT and Facilities remain stubs), OQ-04 unchanged in shape but now genuinely protective, ELIG-03 functional, BRD-014 implementable. Flipped both contested-resolution banners from contested to amended/confirmed, added an amendment index table, and marked AMD-01 resolved. Raised three new consequences: C-09 coverage tier, C-10 opaque idempotency key, C-11 AMD-02. **Raised AMD-02** — `applyTransfer` runs roughly 30+ days before the OQ-06 effective date, so effective-date timing is undefined; recorded the working assumption "apply immediately" with two alternatives, left open for a Gate 0 ruling. Revised the spec to **rev 3**: added `applyTransfer(ApplyTransferCommand)` to the published interface with `ApplyTransferCommand`/`ApplyTransferOutcome` records, a semantics table covering effect, timing, idempotency, atomicity, validation, failure and boundary, an `applied_transfers` idempotency table keyed by an opaque `transfer_request_id` with no foreign key, four acceptance criteria AC20 to AC23 and five tests UT24 to UT28. Raised the module coverage tier from Utility 60 percent to Critical 80 percent in both the spec and `constitution.md`. Restored the rev 2 revision-history row that had been trimmed, leaving the document self-consistent. Updated `architecture.md` §9.9 to resolved, added §9.10 revised dependency direction showing the new acyclic `orchestration → employee-directory` edge and §9.11 for AMD-02. Synced `brd-change-log.md` entry 6, `status.md` blocks 7 and 8 plus the daily log, and `dashboard.html`. No plan, tasks, test cases or code generated — Gate 1 HALT still in force.

### [2026-09-19 12:37]
**User Request:** Rule on AMD-02 by choosing from presented options.
**Agent Action:** Before asking, surfaced a correctness wrinkle affecting all three options — `applyTransfer` sets `role_started_at` and `last_transfer_completed_at` to the effective date, so both hold future dates for ~30 days, and ELIG-03 can fail open unless written as an upper-bound comparison. Presented three options; Gate 0 approver Vaibhaw Soni ruled **Option A — apply immediately** at 12:37:43. Recorded the ruling in `.ai-context/BRD.md` (v1.5) with the rationale that scheduling (Option B) would have left the OQ-04 fan-out reading stale org data, defeating the gate's purpose, and effective-dated records (Option C) were disproportionate. Documented the accepted consequence explicitly: between approval and the effective date the directory reports post-transfer values, recorded as ratified behaviour rather than a defect. Raised consequence **C-12** — ELIG-03 must be `last_transfer_completed_at IS NULL OR last_transfer_completed_at <= today - 12 months`, never a between-range, and ELIG-01 needs no special handling because a future date yields a negative interval. Marked AMD-02 resolved and updated the amendment index; both amendments are now closed and the BRD has no open items. Also repaired a duplicated `## Consequences Arising From These Resolutions` heading left by an earlier splice, and added C-09 through C-12 to that table. Revised the spec to **rev 4**: added AC24 and UT29 pinning the early-visibility behaviour so Gate 2 cannot file it as a defect, restated the timing row as ratified, folded C-12 into the constraints passed forward to the `approval` spec alongside the injectable `Clock` requirement, and cleared the BRD rows from Open Dependencies. Spec now carries 24 acceptance criteria and 29 tests. Updated `architecture.md` §9.11 to resolved and added §9.12 amendment status, `brd-change-log.md` entry 7, `status.md` block 8 plus the daily log, and `dashboard.html`. No plan, tasks, test cases or code generated — Gate 1 HALT still in force.
